<section class="search-filter no-mb">
  <div class="container padding-tb">
    <div class="w-medium mx-auto">
      <h1>Universities in {{data.category.key}}</h1>
      <form method="GET" action="/search/" role="search" class="search-form inner">
        <label><input type="text" class="form-control form-control" placeholder="Type an university..." name="q"></label>
        <button class="btn btn-primary" type="submit" title="Search universities">
          <i class="fa fa-search" aria-hidden="true"></i>
          <span class="sr-only">Search Universities</span>
        </button>
      </form>
    </div>
  </div>
</section>

<section class="uni-results no-mb">
  <div class="container">

    <h4>Total {{ data.universitiesdestination.total}} Universities</h4>
    <ul id="universities" class="list-unstyled row no-gutters">
      {{#each data.universitiesdestination.results}}
      <li class="col-md-4">
        <div class="wrapper">
          <div class="logo mx-auto">
            <a href="{{ universityUrl region.key country.key state.key city.key slug }}" title="{{name}}">
            {{#if logo.version}}
            <img src="//res.cloudinary.com/uguide/image/upload/v{{logo.version}}/{{logo.public_id}}.jpg" alt="{{name}} logo" class="img-fluid"
            /> {{else}}
            <img src="/images/university_placeholder.png" class="img-fluid" /> {{/if}}
            </a>
          </div>
          <h3>
            <a href="{{ universityUrl region.key country.key state.key city.key slug }}" title="{{name}}">{{name}}</a>
          </h3>
          {{#if address}}
          <address>
            <i class="fa fa-map-marker" aria-hidden="true"></i>{{address}}</address>
          {{/if}} {{#if outStudents}}
          <p class="feature">International students: {{outStudents}}</p>
          {{/if}}
           {{#ifCond qs '||' the }}

          <div class="rankings">
            <hr>
            <p>
              <i class="fa fa-trophy" aria-hidden="true"></i>
              University Ranking
            </p>
            <div class="row">
              {{#if qs}}
              <div class="col-6">QS:
                <strong>{{qs}}</strong>
              </div>
              {{/if}} {{#if the}}
              <div class="col-6">THE:
                <strong>{{the}}</strong>
              </div>
              {{/if}} {{#if arwu}}
              <div class="col-6">ARWU:
                <strong>{{arwu}}</strong>
              </div>
              {{/if}} {{#if forbes}}
              <div class="col-6">Forbes:
                <strong>{{forbes}}</strong>
              </div>
              {{/if}} {{#if macleans}}
              <div class="col-6">Macleans:
                <strong>{{macleans}}</strong>
              </div>
              {{/if}} {{#if cug}}
              <div class="col-6">Complete University Guide:
                <strong>{{cug}}</strong>
              </div>
              {{/if}}
            </div>
          </div>
          {{/ifCond }}
        </div>
      </li>
      {{/each}}
    </ul>


    {{!-- <div class="row">
          {{# each data.universitiesdestination.results}}
            {{#if image}}
            <img src="{{{cloudinaryUrl image width=160 height=160 crop=" fit "}}}" class="img-responsive"> {{else}}
            <img src="/images/university_placeholder.png" class="img-responsive" width="100" style="border:1px solid #ccc;"> {{/if}}
          {{/each}}
    </div> --}}

    <div class="pagi-block">
      {{>paginationuniversitybydestination}}
    </div>
  </div>

</section>